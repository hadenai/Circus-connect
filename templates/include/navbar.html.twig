<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home_page') }}">Circus Connect</a>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link ml-4 " href="{{ path('circus_index') }}">Cirque </a>
            </li>
            <li class="nav-item">
                <a class="nav-link ml-5" href="{{ path('clown') }}">Clown</a>
            </li>
            {% if not app.user %}
            <li class="nav-item">
                <a class="nav-link ml-4" href="{{ path('security_registration') }}">s'inscrire</a>
            </li>
            <li>
                <a class="nav-link ml-4" href="{{ path('account_login') }}">connexion</a>
            </li>
            {% endif %}
            {% if app.user %}
            <li>
                <a class="nav-link ml-4" href="{{ path('app_logout') }}">déconnexion</a>
            </li>
            {% endif %}

        </ul>
    </div>
</nav>